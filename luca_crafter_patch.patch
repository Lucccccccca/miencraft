*** Begin Patch
*** Update File: src/main/java/de/luca/plugin/AlwaysDropListener.java
@@ public void onBlockBreak(BlockBreakEvent e) {
-            block.getWorld().dropItemNaturally(block.getLocation(), new org.bukkit.inventory.ItemStack(type));
+            // Prevent standard drops (e.g., obsidian from Ender chests) and drop the block itself
+            e.setDropItems(false);
+            block.getWorld().dropItemNaturally(block.getLocation(), new org.bukkit.inventory.ItemStack(type));
*** End Patch
*** Update File: src/main/java/de/luca/plugin/BaumCommand.java
@@ public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
         Player p = (Player) sender;
         openMenu(p);
+        // Notify the player that the tree-felling menu was opened
+        p.sendMessage(ChatColor.GREEN + "🌲 Das Baumfäller-Menü wurde geöffnet.");
         return true;
*** End Patch
*** Update File: src/main/java/de/luca/plugin/ErzCommand.java
@@ public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
         if (!(sender instanceof Player p)) return true;
 
         openErzMenu(p);
-        return true;
+        // Notify the player that the ore system menu was opened
+        p.sendMessage(ChatColor.AQUA + "⛏ Das Erz-System-Menü wurde geöffnet.");
+        return true;
*** End Patch
*** Update File: src/main/java/de/luca/plugin/LucaCrafterPlugin.java
@@
-import org.bukkit.Bukkit;
-import org.bukkit.Server;
-import org.bukkit.plugin.java.JavaPlugin;
+import org.bukkit.Bukkit;
+import org.bukkit.Server;
+import org.bukkit.plugin.java.JavaPlugin;
+import org.bukkit.ChatColor;
@@ public void onEnable() {
         Bukkit.getPluginManager().registerEvents(new JoinListener(), this);
 
         getLogger().info("✅ LucaCrafterPlugin erfolgreich aktiviert!");
+
+        // 📢 Stündliche Erinnerung an die Hauptfunktionen
+        long hour = 20L * 60 * 60; // 20 ticks per second * 60 seconds * 60 minutes
+        Bukkit.getScheduler().runTaskTimer(this, () -> {
+            String msg = ChatColor.YELLOW + "💡 Tipp: Nutze "
+                    + ChatColor.GREEN + "/baum" + ChatColor.YELLOW + " für den Baumfäller, "
+                    + ChatColor.AQUA + "/erz" + ChatColor.YELLOW + " für das Erz-System "
+                    + ChatColor.GOLD + "/settings" + ChatColor.YELLOW + " für deine Einstellungen.";
+            Bukkit.broadcastMessage(msg);
+        }, hour, hour);
*** End Patch
*** End Patch
